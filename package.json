{
  "name": "mec",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix uafms\"",
    "dev-backend": "npm run dev --prefix backend",
    "dev-frontend": "npm run dev --prefix uafms",
    "build:all": "npm run build --prefix uafms",
    "start:prod": "concurrently \"npm run start --prefix backend\" \"npm run start --prefix uafms\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
